# Tasks: controller_portal_register

- [x] T1 - Implement `registerPortalClient` controller function with schema validation for name and phone number. Covers: R1, R2, R3.
- [x] T2 - Integrate the controller with the captive portal database model's upsert method for valid payloads. Covers: R4.
- [x] T3 - Add integration tests asserting validation rules, 400 error packets for bad inputs, and successful parsing in `tests/integration/controller_portal_register.integration.test.ts`. Covers: R5.
