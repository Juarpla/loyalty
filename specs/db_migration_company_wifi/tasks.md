# Tasks

- [x] T1 - Generate a new Supabase migration file to create the `companies` and `company_wifi_settings` tables with their respective columns, constraints, unique indexes, and defaults. Covers: R1, R2, R3, R4, R5.
- [x] T2 - Implement integration tests in `tests/integration/db_migration_company_wifi.test.ts` to assert the existence of the tables, the columns and their constraints, the foreign key relation, the unique constraint, and default values. Covers: R6.
