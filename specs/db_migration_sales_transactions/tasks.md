# Tasks

- [x] T1 - Generate and write Supabase SQL migration for `sales_transactions` table with columns and `phone_number` B-Tree index. Covers: R1, R2, R3.
- [x] T2 - Scaffold and write integration test `db_migration_sales.test.ts` to assert schema, column types, and indexes. Covers: R1, R2, R3.
