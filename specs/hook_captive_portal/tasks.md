# Tasks: Captive Portal State React Hook

- [x] T1 - Implement the `usePortal` custom hook skeleton and state variables (`isLoading`, `isSuccess`, `error`) in `src/hooks/use-portal.hook.ts`. Covers: R2, R3, R4, R5.
- [x] T2 - Implement the `registerClient` fetch logic inside the hook to submit data to `/api/v1/portal/register` and update states accordingly. Covers: R1, R2, R3, R4.
- [x] T3 - Write integration tests in `tests/integration/hook_captive_portal.integration.test.ts` mocking the `fetch` API to verify all state transitions (loading, success, error). Covers: R1, R2, R3, R4.
